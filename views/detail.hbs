{{!-- <h2>{{product.title}}</h2> --}}

{{!-- <p><img src={{product.image}}></p>
<P>{{product.price}}</P>

<p>{{product.description}}</p> --}}
{{!-- <button type="button" class="btn btn-outline-success">Add Product</button> --}}

<div class="row">
    {{each}}
  <div class="col-sm-6 mb-3 mb-sm-0">
    <div >
    <img src={{product.image}} class="card">
      <div class="card-body">
        <h5 class="card-title">{{product.title}}</h5>
        <p>{{product.price}}</p>
        <p class="card-text">{{product.description}}</p>
        <button type="button" class="btn btn-outline-success">Add Product</a>
      </div>
    </div>
  </div>
  <div class="col-sm-6">
    <div> 
     <img src={{product.image}} class="card">class="card">
      <div class="card-body">
        <h5 class="card-title">{{product.title}}</h5>
        <p class="card-text">{{product.description}}</p>
       <button type="button" class="btn btn-outline-success">Add Product</a>
      </div>
    </div>
  </div>
  {{each}}
</div>